(function (factory) {
	var root = (function () { return this })();

	if (typeof exports === 'object') {
		module.exports = factory.call(root{{#if cjsDependencies}}, {{{cjsDependencies}}}{{/if}});
	} else
	if (typeof define === 'function' && define.amd) {
		define({{#if amdModuleId}}'{{amdModuleId}}', {{/if}}[{{{amdDependencies}}}], function () {
			factory.apply(root, arguments);
		});
	}
	else {
		{{#if globalAlias}}root['{{globalAlias}}'] = {{/if}}factory.call(root{{#if globalDependencies}}, {{{globalDependencies}}}{{/if}});
	}
}(function({{dependencies}}) {

'use strict';

var global = this;

{{{code}}}{{#if objectToExport}}
return {{objectToExport}};{{/if}}

}));
